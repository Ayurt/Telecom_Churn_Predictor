# Minimal runtime requirements for Telecom Churn Predictor
# Keep versions as conservative minimums for compatibility on Windows
streamlit>=1.20
pandas>=1.5
numpy>=1.23
scikit-learn>=1.2
xgboost>=1.7
joblib>=1.2
scipy>=1.9

# Optional (useful for further analysis/visuals)
matplotlib>=3.5
seaborn>=0.11

# Notes:
# - Run `pip install -r requirements.txt` to install.
# - If you need strict reproducibility, let me know and I'll pin exact versions from your environment.